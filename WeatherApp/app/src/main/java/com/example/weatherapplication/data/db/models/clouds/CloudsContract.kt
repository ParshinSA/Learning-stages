package com.example.weatherapplication.data.db.models.clouds

object CloudsContract {
    const val TABLE_NAME = "clouds_table"

    object Columns {
        const val PK_QUANTITY = "pK_quantity(%)"
    }
}